pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                echo 'Pipeline is working!'
                sh 'echo "Hello from Jenkins"'
            }
        }
    }
}
